/**
 * Default API keys configuration
 * These keys are provided as a convenience for users who don't have their own API keys
 */
export declare const defaultApiKeys: {
    openai: string;
    stabilityai: string;
    replicate: string;
};
