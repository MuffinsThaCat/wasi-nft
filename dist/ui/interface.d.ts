/**
 * User interface for the digital assets application
 */
import { AssetManager } from '../core/assetManager';
/**
 * Initialize the user interface
 */
export declare function initializeUI(assetManager: AssetManager): void;
